import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CardTurmasComponent } from './cartao-tempos-lectivos.component';

describe('CardTurmasComponent', () => {

  it('navbar directive set', ()=>{
    expect(true).toBe(true);
  });

  /*
  let component: CardTurmasComponent;
  let fixture: ComponentFixture<CardTurmasComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ CardTurmasComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(CardTurmasComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });

  it('true to be true', ()=>{
    expect(true).toBe(true);
  })
  */
});
